# Oracle connection template for collaborators.
# Copy this file to `.env` and fill in the values for your Oracle 12c/19c account.

ORACLE_HOST=your-hostname
ORACLE_PORT=1521

# Fill one of SERVICE_NAME or SID (leave the other blank).
ORACLE_SERVICE_NAME=your-service-name
ORACLE_SID=

ORACLE_USER=your_username
ORACLE_PASSWORD=your_password

# Optional ready-made EZCONNECT strings. Use whichever style your DBA provides.
ORACLE_EZCONNECT_SERVICE=//your-hostname:1521/your-service
ORACLE_EZCONNECT_SID=//your-hostname:1521:YOUR_SID

# If you already have a full connect descriptor, set it here instead.
ORACLE_CONNECT_STRING=

# App port for the Node/Express server.
APP_PORT=3000
